# ─── App ──────────────────────────────────────────────────────────────────────
APP_ENV=development
SECRET_KEY=9607f5cf3db7c6594039be01e70bd1f73dacae4a2e4665fa5fb9024c950347f3
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# ─── PostgreSQL ───────────────────────────────────────────────────────────────
POSTGRES_USER=twinai
POSTGRES_PASSWORD=twinai_dev_password
POSTGRES_DB=twinai_db
DATABASE_URL=postgresql+asyncpg://twinai:twinai_dev_password@db:5432/twinai_db

# ─── Redis / Celery ───────────────────────────────────────────────────────────
REDIS_URL=redis://redis:6379/0
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/1

# ─── ChromaDB ─────────────────────────────────────────────────────────────────
CHROMA_HOST=chromadb
CHROMA_PORT=8000

# ─── AI Provider Switch ───────────────────────────────────────────────────────
# Set to "nim" to use NVIDIA NIM, or "gemini" to use Google Gemini
LLM_PROVIDER=nim

# ─── NVIDIA NIM (active when LLM_PROVIDER=nim) ────────────────────────────────
NIM_BASE_URL=https://integrate.api.nvidia.com/v1
NIM_LLM_API_KEY=nvapi-your-llm-key-here
NIM_EMBED_API_KEY=nvapi-your-embed-key-here
NIM_MULTIMODAL_API_KEY=nvapi-your-multimodal-key-here
NIM_OCR_API_KEY=nvapi-your-ocr-key-here
NIM_RERANK_API_KEY=nvapi-your-rerank-key-here
# NIM model names (defaults — change only if using a different NIM model)
LLM_MODEL=meta/llama-4-maverick-17b-128e-instruct
EMBEDDING_MODEL=nvidia/llama-3.2-nemoretriever-300m-embed-v1
MULTIMODAL_MODEL=microsoft/phi-4-multimodal-instruct
RERANK_MODEL=nvidia/rerank-qa-mistral-4b

# ─── Gemini (active when LLM_PROVIDER=gemini) ─────────────────────────────────
GEMINI_API_KEY=your-gemini-api-key-here
# When switching to Gemini, also set:
# LLM_MODEL=models/gemini-2.0-flash
# EMBEDDING_MODEL=models/gemini-embedding-001
# MULTIMODAL_MODEL=models/gemini-2.0-flash

# ─── Gupshup (WhatsApp Provider) ─────────────────────────────────────────────
# GUPSHUP_MODE=mock during dev (Meta approval can take up to 6 weeks).
# Switch to real once credentials are obtained.
GUPSHUP_MODE=mock
GUPSHUP_API_KEY=your-gupshup-api-key
GUPSHUP_APP_NAME=your-app-name
GUPSHUP_SENDER_NUMBER=+91XXXXXXXXXX
GUPSHUP_WEBHOOK_SECRET=your-hmac-webhook-secret

# ─── SendGrid (Owner email alerts) ───────────────────────────────────────────
SENDGRID_API_KEY=your-sendgrid-api-key
ALERT_EMAIL_FROM=alerts@yourdomain.com
OWNER_ALERT_EMAIL=owner@yourdomain.com

# ─── Sentry ───────────────────────────────────────────────────────────────────
SENTRY_DSN=

# ─── CORS ─────────────────────────────────────────────────────────────────────
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# ─── Uploads ──────────────────────────────────────────────────────────────────
UPLOAD_DIR=/app/uploads
MAX_UPLOAD_SIZE_MB=20

# ─── pgAdmin (DB admin UI) ────────────────────────────────────────────────────
# TC-030 fix: credentials moved out of docker-compose.yml.
# IMPORTANT: Set a strong password — port 5050 has full DB access!
PGADMIN_EMAIL=admin@yourdomain.com
PGADMIN_PASSWORD=change_me_to_a_strong_password

# ─── WhatsApp Messaging ───────────────────────────────────────────────────────
# Set to "gupshup" or "twilio"
MESSAGING_PROVIDER=gupshup

# ─── Twilio (active when MESSAGING_PROVIDER=twilio) ──────────────────────────
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your-twilio-auth-token
TWILIO_WHATSAPP_NUMBER=+14155238886

# ─── Owner ────────────────────────────────────────────────────────────────────
# The WhatsApp number of the business owner (receives commands + alerts)
OWNER_PHONE=+91XXXXXXXXXX
# The support phone shown to clients in RAG bot fallback messages
# Defaults to OWNER_PHONE if not set
SUPPORT_PHONE=+91XXXXXXXXXX

# ─── Guardrails ───────────────────────────────────────────────────────────────
# Layer 1: Input controls
MAX_MESSAGE_LENGTH=1000          # Characters; messages longer than this are rejected
MAX_MESSAGES_PER_HOUR=20        # Per-user hourly rate limit (sliding window via Redis)

# Layer 3: Output controls
MAX_RESPONSE_LENGTH=1500        # LLM responses longer than this are truncated with a CTA

# Layer 4: Token / cost controls
# Set DAILY_TOKEN_LIMIT to a positive integer to cap total LLM usage per day.
# 0 = unlimited (safe default; no API calls are blocked).
DAILY_TOKEN_LIMIT=0
PER_USER_DAILY_TOKEN_LIMIT=5000  # Per-user cap; prevents a single user burning the budget
GUARDRAIL_ALERT_THRESHOLD=0.8    # Owner is alerted when usage exceeds this fraction

# Layer 2: RAG quality gate
RAG_MIN_SIMILARITY_SCORE=0.45   # ChromaDB distance below this → use fallback, not LLM

